기본적으로 n =3 , 9 , 27... 이 커져도 언제나 크기를
3x3 =9 개의 부분으로 나눌 수 있다.

코드를 살펴보면 star 는 ["***", "* *", "***"] 으로
초기화 되어 있다.

n = 3인 경우에는 e = 0 이 되어서 바로 star를 출력하게 된다
n = 9인 경우에는 e = 1 이 되어서  첫 번째 for문을 한 번 돌게 된다
이 때 n의 크기에 따라 다르지만 중간에 공백이 발생 한다는 규칙을 찾을 수 있다.
즉 i // len(n) == 1 (여기서 n은 star를 매개변수로 입력받는다) 일 때 공백이 발생한다.

n = 27인 경우에도 마찬가지로 e = 2가 되어서 첫 번째 for문을 두 번 돌게 된다.
첫 번째 for문을 한번 돌면 star에 n=9인 별값들이 모여있다. 이것을 매개변수로 하여 다시 한번
stars함수에 들어가면 n = 27을 구할 수 있다.